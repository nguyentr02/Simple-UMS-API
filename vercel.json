{
  "version": 2,
  "buildCommand": "npx prisma generate && npx prisma migrate deploy",
  "builds": [
    {
      "src": "src/app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/app.js"
    }
  ]
}